<configuration>
  <!-- Mono DLL mapping for KitsuneCommand native dependencies -->
  <!-- SQLite: handled by System.Data.SQLite.dll.config (P/Invoke target is "sqlite3") -->
  <!-- SkiaSharp: handled by runtime DLL map in ModEntry.cs -->
</configuration>

namespace KitsuneCommand.Features
{
    /// <summary>
    /// Configuration settings for the Task Scheduling feature.
    /// Tasks are fully DB-driven, so no additional config is needed.
    /// </summary>
    public class TaskScheduleSettings
    {
    }
}
